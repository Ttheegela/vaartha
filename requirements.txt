# GeoSentinel Terminal — VARTA
# Team 7 Lambda | SP2026 Final Project
# Install: pip install -r requirements.txt
# Verified on macOS ARM64, Python 3.12

# ── Frontend ──────────────────────────────────────────────────────────────────
streamlit>=1.42.0
plotly>=5.22.0
folium>=0.16.0
streamlit-folium>=0.22.0

# ── Data ──────────────────────────────────────────────────────────────────────
polars>=0.20.0
duckdb>=0.10.0
pyarrow>=14.0.2
numpy>=1.26.4

# ── Data Sources ──────────────────────────────────────────────────────────────
yfinance>=0.2.66
fredapi>=0.5.0
requests>=2.32.2
python-dotenv>=1.0.0
pytrends>=4.9.0          # Google Trends via unofficial API (graceful fallback if unavailable)

# ── Machine Learning ──────────────────────────────────────────────────────────
scikit-learn>=1.4.0
xgboost>=2.0.0
hmmlearn>=0.3.0

# ── Kronos Foundation Model (shiyu-coder/Kronos, AAAI 2026) ──────────────────
# NeoQuasar/Kronos-small — OHLCV decoder-only Transformer, 24.7M params
# Clone separately:  git clone https://github.com/shiyu-coder/Kronos src/models/kronos_repo
# Requires: torch>=2.2.0 (MPS backend for Apple Silicon)
torch>=2.2.0
transformers>=4.40.0

# ── Utilities ─────────────────────────────────────────────────────────────────
pycountry>=26.2.16
pandas>=2.0.0          # transitive dep for yfinance — do not use directly in app code (use Polars)
xlrd>=2.0.1            # required by pandas to parse .xls files (Caldara-Iacoviello GPR index)

# ── Brokerage ─────────────────────────────────────────────────────────────────
alpaca-py>=0.26.0
